name: Cron Job

on:
  schedule:
    - cron: '*/30 * * * *' # Run every 30 minutes
  workflow_dispatch: # Allow manual triggering

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Cron API
        run: |
          curl -X GET ${{ secrets.APP_URL }}/api/reminders/cron \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
